This directory contains built-in Digital Employee skills grouped by employee ID.

Each subdirectory represents a Digital Employee type/ID and should contain one or more
`SKILL.md` files following the standard OpenOcta skill format.

